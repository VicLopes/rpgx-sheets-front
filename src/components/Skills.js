import React, { useEffect, useState } from 'react'
import PropTypes from 'prop-types'
import '../styles/skills.scss'

export const Skills = ({ skillsObj }) => {
  return (
    null
  )
}

Skills.PropTypes = {
  skillsObj: PropTypes.object,
}